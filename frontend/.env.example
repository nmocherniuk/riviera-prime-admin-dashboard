VITE_API_URL=http://localhost:5000/api
# Optional; must match backend PUBLIC_BOOKING_API_KEY if that env is set (dev only if exposed in client).
# VITE_PUBLIC_BOOKING_API_KEY=

# Stripe publishable key (safe for client-side)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
